const { getNameCases } = require('./getNameCases');
const { getQuestions } = require('./getQuestions');
const { getVariables } = require('./getVariables');
const { parseAnswers } = require('./parseAnswers');

module.exports = {
  getNameCases,
  getQuestions,
  getVariables,
  parseAnswers,
};
